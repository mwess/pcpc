1. mehrere bilder 
mehrere bilder koennen jetzt eingelesen werden. dazu wird jeder dateiname durch einen integer ersetzt (es wird bei 0 angefangen zu zaehlen). 
beim qsub befehl wird jetzt nicht mehr die anzahl aller nodes uebergeben, sondern (#nodes/#bilder). 
beim zusammensetzen werden jetzt die bilder anhand ihres identifiers zusammengesetzt.
dann werden die bilder vom identifier zum richtigen namen wieder zusammengesetzt


2. error handling
die jobstrings werden in einem jobarray gesammelt.
die jobs werden solange gesendet bis alle teilbilder gerendert wurden. ist ein teilbild nicht gerendet,dann wird der entsprechende job neu gesendet.
